{% extends('base.html.twig') %}

{% block content %}
    <br />
    <center>
        {{ form_start(form, { 'attr': { 'id': 'discussion_form' } }) }}
        <table class="design">
            {{ form_row(form.title) }}
            {{ form_row(form.text) }}
            {{ form_row(form.signatureOn) }}
            <tr>
                <td>&nbsp;</td>
                <td>
                    <div class="button_clear"><div class="somda-button-left"><div class="blank_home"><a href="#" onclick="$('#discussion_form').submit(); return false;"><span>Start nieuwe discussie</span></a></div></div></div>
                    <div class="button_clear"><div class="somda-button-left"><div class="blank_home"><a href="{{ path('forum_forum', { 'id': forum.id, 'name': (forum.name | url_encode) }) }}"><span>Terug naar de forum-categorie</span></a></div></div></div>
                </td>
            </tr>
        </table>
        {{ form_end(form) }}
    </center>
    <br />
{% endblock %}
