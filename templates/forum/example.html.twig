<table align="center">
    <tr><th class="left-align" colspan="2"><i class="color-orange fas fa-comment-dots"></i> {{ "now" | date('d-m-Y H:i:s') }}</th></tr>
    <tr>
        <td class="info" nowrap="nowrap">
            {{ user.id | displayUser(user.username) | raw }}
            {% if user.info.avatar | length > 0 %}
                <br /><img alt="{{ user.username }}" src="{{ asset('images/avatar/' ~  user.info.avatar) }}" />
            {% endif %}
            {% if user.info.city | length > 0 %}<br />{{ user.info.city }}{% endif %}
            <br />
            {% if user.info.twitterAccount | length > 0 %}
                <a href="https://twitter.com/#!/{{ user.info.twitterAccount }}" rel="ugc noopener" target="_blank"><i class="fab fa-twitter" title="Twitter {{ user.info.twitterAccount }}"></i></a>
            {% endif %}
            {% if user.info.facebookAccount | length > 0 %}
                <a href="https://www.facebook.com/{{ user.info.facebookAccount }}" rel="ugc noopener" target="_blank"><i class="fab fa-facebook" title="Facebook {{ user.info.facebookAccount }}"></i></a>
            {% endif %}
            {% if user.info.youtubeAccount | length > 0 %}
                <a href="https://www.youtube.com/{{ user.info.youtubeAccount }}" rel="ugc noopener" target="_blank"><i class="fab fa-youtube" title="Youtube {{ user.info.youtubeAccount }}"></i></a>
            {% endif %}
            {% if user.info.flickrAccount | length > 0 %}
                <a href="https://www.flickr.com/photos/{{ user.info.flickrAccount }}" rel="ugc noopener" target="_blank"><i class="fab fa-flickr" title="Flickr {{ user.info.flickrAccount }}"></i></a>
            {% endif %}
        </td>
        <td class="text" id="post_example">
            <p class="info">Hier komt een voorbeeld van jouw reactie, dit wordt automatisch bijgewerkt als je even niet typt</p>
        </td>
    </tr>
</table>
