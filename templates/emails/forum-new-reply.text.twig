Op het forum van Somda ({{ absolute_url(path('home')) }}) is een reactie geplaatst in de discussie "{{ discussion.title | url_encode }}".

Bekijk hier naar de forumdiscussie: {{ absolute_url(path('forum_discussion', { 'id': discussion.id, 'name': (discussion.title | url_encode) })) }}
