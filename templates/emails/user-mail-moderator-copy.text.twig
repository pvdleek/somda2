Onderstaande e-mail is via Somda ({{ absolute_url(path('home')) }}) door een moderator verzonden naar gebruiker "{{ user.username }}".

---------------------------
{{ text | raw }}
---------------------------
