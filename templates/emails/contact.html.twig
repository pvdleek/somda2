<picture><source srcset="{{ absolute_url(asset('images/logo-1.webp')) }}" /><img alt="Somda" src="{{ absolute_url(asset('images/logo-1.webp')) }}" /></picture>
<br />
<br />Feedback van {% if user is not null %}{{ user.username }} ({{ user.email }}){% else %}{{ emailAddress }}{% endif %}
<br />
<br />De gebruiker heeft dit bericht opgegeven:
<br />{{ text }}
