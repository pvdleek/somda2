{% extends('base.html.twig') %}

{% block content %}
    <br />
    <div class="home-panel separate-panel">
        <div class="home-panel-title">Niet geimplementeerd</div>
        <div class="home-panel-content">
            <strong>Deze functie is helaas nog niet geimplementeerd in de nieuwe Somda</strong>
            <br />
            <br />
            <div class="home-panel-buttons">
                <a class="somda-button" href="{{ path('home') }}">Terug naar de homepagina</a>
                <a class="somda-button" href="{{ path('contact') }}">Neem contact op met de webmaster</a>
            </div>
        </div>
    </div>
    <br />
    <br />
{% endblock %}
