{% extends('base.html.twig') %}

{% block content %}
    <div style="float:left; width:240px;">
        <table class="design">
            <tr><th colspan="8">{{ 'location.search.character' | trans | raw }}</th></tr>
            <tr>
                {% for i in 0..25 %}
                    {% if ((i / 8) | round) == i / 8 and i != 0 %}</tr><tr>{% endif %}
                    <td>&nbsp;<a href="#" onclick="searchCharacter('{{ (i + 65) | chr }}'); return false;">{{ (i + 65) | chr }}</a>&nbsp;</td>
                {% endfor %}
                <td colspan="6">&nbsp;<a href="#" onclick="searchCharacter('%'); return false;">{{ 'location.search.characterAll' | trans }}</a></td>
            </tr>
            <tr><td colspan="8" style="background-color:#FFFFFF;">&nbsp;</td></tr>
            <tr><th colspan="8"><label for="textSearchSingle">{{ 'location.search.single' | trans }}</label></th></tr>
            <tr>
                <td colspan="5"><input id="textSearchSingle" maxlength="10" name="textSearchSingle" onkeypress="return checkEnter(event, 'searchSingle');" size="12" type="text" value="{{ search }}" /></td>
                <td colspan="3"><a class="somda-button" href="#" id="searchSingle" onclick="searchSingle(); return false;" title="{{ 'location.search.single' | trans }}"><span>{{ 'general.button.search' | trans }}</span></a></td>
            </tr>
            <tr><td colspan="8" style="background-color:#FFFFFF;">&nbsp;</td></tr>
            <tr><th colspan="8"><label for="textSearchName">{{ 'location.search.name' | trans }}</label></th></tr>
            <tr>
                <td colspan="5"><input id="textSearchName" maxlength="10" name="textSearchName" onkeypress="return checkEnter(event, 'searchName');" size="12" type="text" value="{{ search }}" /></td>
                <td colspan="3"><a class="somda-button" href="#" id="searchName" onclick="searchName(); return false;" title="{{ 'location.search.name' | trans }}"><span>{{ 'general.button.search' | trans }}</span></a></td>
            </tr>
            <tr><td colspan="8" style="background-color:#FFFFFF;">&nbsp;</td></tr>
            <tr><th colspan="8"><label for="textSearchDescription">{{ 'location.search.description' | trans }}</label></th></tr>
            <tr>
                <td colspan="5"><input id="textSearchDescription" maxlength="15" name="textSearchDescription" onkeypress="return checkEnter(event, 'searchDescription');" size="12" type="text" value="{{ search }}" /></td>
                <td colspan="3"><a class="somda-button" href="#" id="searchDescription" onclick="searchDescription(); return false;" title="{{ 'location.search.description' | trans }}"><span>{{ 'general.button.search' | trans }}</span></a></td>
            </tr>
        </table>
    </div><div style="float:left; width:710px;">
        {% if locations %}
            <table class="design" id="sorted">
                <thead><tr><th>{{ 'location.name' | trans }}</th><th>{{ 'location.description' | trans }}</th><th>{{ 'location.category' | trans }}</th></tr></thead>
                {% for location in locations %}
                    <tr><td>{{ location.name }}</td><td>{{ location.description }}</td><td>{{ location.category.name }}</td></tr>
                {% endfor %}
            </table><br />
        {% else %}
            <p class="warn">{{ 'location.search.noResults' | trans }}</p>
        {% endif %}

        {% if locations %}

            <link rel="stylesheet" href="https://unpkg.com/leaflet@1.3.4/dist/leaflet.css" integrity="sha512-puBpdR0798OZvTTbP4A8Ix/l+A4dHDD0DGqYW6RQ+9jxkRFclaxxQb/SJAWZfWAkuyeQUytO7+7N4QKrDh+drA==" crossorigin=""/>
            <script src="https://unpkg.com/leaflet@1.3.4/dist/leaflet.js" integrity="sha512-nMMmRyTVoLYqjP9hrbed9S+FzjZHW5gY1TWCHA5ckwXZBadntCNs8kEqAWdrb9O7rxbCaA4lKTIWjDXZxflOcA==" crossorigin=""></script>
            <div id="mapid" style="height:700px; margin-left:10px; width:620px;"></div>
{#            <br /><div id="map_canvas" style="height:700px; margin-left:10px; width:620px;"></div>#}
            <p class="info" style="margin-left:10px; width:600px;">{{ 'location.credits' | trans | raw }}<br />{{ 'location.additions' | trans | format('http://www.somda.nl/forum/12369/Co%F6rdinaten+van+verkortingen+voor+de+kaart/') | raw }}</p>
        {% endif %}
    </div>
{% endblock %}

{% block javascripts %}
    <script src="https://maps.googleapis.com/maps/api/js?sensor=false" type="text/javascript"></script>
    <script type="text/javascript">
        function searchCharacter(character) {
            const location = '{{ path('location_search', { 'searchMethod': constant('App\\Controller\\InformationController::SEARCH_METHOD_CHARACTER'), 'search': 'searchCharacter' }) }}';
            document.location = location.replace('searchCharacter', character);
        }

        function searchSingle() {
            const location = '{{ path('location_search', { 'searchMethod': constant('App\\Controller\\InformationController::SEARCH_METHOD_SINGLE'), 'search': 'searchSingle' }) }}';
            document.location = location.replace('searchSingle', document.getElementById('textSearchSingle').value);
        }

        function searchName() {
            const location = '{{ path('location_search', { 'searchMethod': constant('App\\Controller\\InformationController::SEARCH_METHOD_NAME'), 'search': 'searchName' }) }}';
            document.location = location.replace('searchName', document.getElementById('textSearchName').value);
        }

        function searchDescription() {
            const location = '{{ path('location_search', { 'searchMethod': constant('App\\Controller\\InformationController::SEARCH_METHOD_DESCRIPTION'), 'search': 'searchDescription' }) }}';
            document.location = location.replace('searchDescription', document.getElementById('textSearchDescription').value);
        }

        {% if locations %}
            $(document).ready(function() {
                $('#sorted').tablesorter({sortList: [[0, 0]]});

                {% if locations | length == 1 %}
                    {% set latitude = (locations | first).latitude %}
                    {% set longitude = (locations | first).longitude %}
                    {% set zoomLevel = 16 %}
                {% else %}
                    {% set latitude = 52.315195 %}
                    {% set longitude = 5.634155 %}
                    {% set zoomLevel = 8 %}
                {% endif %}

                let mymap = L.map('mapid').setView([{{ latitude }}, {{ longitude }}], {{ zoomLevel }});
                L.tileLayer('https://api.tiles.mapbox.com/v4/{id}/{z}/{x}/{y}.png?access_token=pk.eyJ1IjoicHZkbGVlayIsImEiOiJjanE1MTlvOGQxdHpyM3lueWdjYmVtOHB6In0.z1E_Bl8X0W5ik_upQgR7Mw', {
                    maxZoom: 18,
                    attribution: 'Map data &copy; <a href="https://www.openstreetmap.org/">OpenStreetMap</a> contributors, ' +
                        '<a href="https://creativecommons.org/licenses/by-sa/2.0/">CC-BY-SA</a>, ' +
                        'Imagery © <a href="https://www.mapbox.com/">Mapbox</a>',
                    id: 'mapbox.streets'
                }).addTo(mymap);

                {% if locations | length == 1 %}
                    let marker1 = L.marker([{{ latitude }}, {{ longitude }}]).addTo(mymap);
                    marker1.bindPopup("{{ (locations | first).description }} ({{ (locations | first).name }})").openPopup();
                {% else %}
                    {% set counter = 1 %}
                    {% for location in locations %}
                        {% if location.latitude %}
                            let marker{{ counter }} = L.marker([{{ location.latitude }}, {{ location.longitude }}]).addTo(mymap);
                            marker{{ counter }}.bindPopup("{{ location.description }}");
                            {% set counter = counter + 1 %}
                        {% endif %}
                    {% endfor %}
                {% endif %}

    // if ($do_all) {
    //     $query = 'select afkid, afkorting, description, latitude, longitude from '.DB_PREFIX.'_verk where not latitude IS NULL';
    //     $dbset_verk = $db->query($query);
    //     $counter = 1;
    //     while(list($afkid, $afkorting, $description, $latitude, $longitude) = $db->fetchRow($dbset_verk)) {
    //         echo 'var marker', $counter, ' = L.marker([', $latitude, ',', $longitude, ']).addTo(mymap);';
    //         echo 'marker', $counter, '.bindPopup("', $description, ' (', $afkorting, ')");';
    //         ++$counter;
    //     }
    // }

                {#let myOptions = {#}
                {#    center: new google.maps.LatLng({{ latitude }}, {{ longitude }}),#}
                {#    mapTypeId: google.maps.MapTypeId.HYBRID,#}
                {#    zoom: {{ zoomLevel }}#}
                {#};#}
                {#let map = new google.maps.Map(document.getElementById('map_canvas'), myOptions);#}

                {#{% set counter = 1 %}#}
                {#{% for location in locations %}#}
                {#    {% if location.latitude %}#}
                {#        let marker{{ counter }} = new google.maps.Marker({ position: new google.maps.LatLng({{ location.latitude }}, {{ location.longitude }}), map: map, title:"{{ location.name }}: {{ location.description }}" });#}
                {#        {% set counter = counter + 1 %}#}
                {#    {% endif %}#}
                {#{% endfor %}#}
            });
        {% endif %}
    </script>
{% endblock %}
