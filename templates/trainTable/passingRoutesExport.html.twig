{% extends('baseExport.html.twig') %}

{% block content %}
    Doorkomststaat voor {{ trainTableIndex.name }} van {{ locationName }} op {{ ('general.date.days.' ~ dayNumber) | trans | lower }} van {{ startTime }} tot {{ endTime }}
    <br />
    <br />

    {% if passingRoutes | length %}
        {% include 'trainTable/passingRoutesTable.html.twig' %}
    {% else %}
        Er zijn geen passerende treinen gevonden
    {% endif %}
{% endblock %}
