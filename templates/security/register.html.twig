{% extends 'base.html.twig' %}

{% block content %}
    <br />
    <center>
        {{ form_start(form, { 'attr': { 'id': 'user_form' } }) }}
            <table class="design">
                {{ form_row(form.email) }}
                {{ form_row(form.username) }}
                {{ form_row(form.plainPassword) }}
                <tr>
                    <td>&nbsp;</td>
                    <td><a class="somda-button" href="#" onclick="$('#user_form').submit(); return false;"><span>Registereren bij Somda</span></a></td>
                </tr>
            </table>
        {{ form_end(form) }}
    </center>
    <br />
{% endblock %}
