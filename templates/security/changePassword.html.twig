{% extends 'base.html.twig' %}

{% block content %}
    <div class="home-panel separate-panel">
        <div class="home-panel-title">Wachtwoord wijzigen</div>
        <div class="home-panel-content">
            {{ form_start(form, { 'attr': { 'id': 'user_form' } }) }}
                <table class="design">
                    {{ form_row(form.currentPassword) }}
                    {{ form_row(form.newPassword) }}
                    <tr>
                        <td>&nbsp;</td>
                        <td><a class="somda-button" href="#" onclick="$('#user_form').submit(); return false;"><span>Wachtwoord wijzigen</span></a></td>
                    </tr>
                </table>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
