{% extends 'base.html.twig' %}

{% block content %}
    <br />
    <center>
        {{ form_start(form, { 'attr': { 'id': 'user_form' } }) }}
            <table class="design">
                {{ form_row(form.key) }}
                <tr>
                    <td>&nbsp;</td>
                    <td><a class="somda-button" href="#" onclick="$('#user_form').submit(); return false;">Activeer mijn account bij Somda</a></td>
                </tr>
            </table>
        {{ form_end(form) }}
    </center>
    <br />
{% endblock %}
