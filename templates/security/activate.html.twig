{% extends 'base.html.twig' %}

{% block content %}
    <br />
    <center>
        {{ form_start(form, { 'attr': { 'id': 'user_form' } }) }}
            <table class="design">
                {{ form_row(form.key) }}
                <tr>
                    <td>&nbsp;</td>
                    <td><a class="somda-button" href="#" onclick="$('#user_form').submit(); return false;"><span>Activeer mijn account bij Somda</span></a></td>
                </tr>
            </table>
        {{ form_end(form) }}
    </center>
    <br />
{% endblock %}
