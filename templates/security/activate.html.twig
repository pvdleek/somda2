{% extends 'base.html.twig' %}

{% block content %}
    <br />
    <center>
        {{ form_start(form, { 'attr': { 'id': 'user_form' } }) }}
            <table class="design">
                {{ form_row(form.key) }}
                <tr>
                    <td>&nbsp;</td>
                    <td><div class="button_clear"><div class="somda-button-left"><div class="blank_home"><a href="#" onclick="$('#user_form').submit(); return false;"><span>Activeer mijn account bij Somda</span></a></div></div></div></td>
                </tr>
            </table>
        {{ form_end(form) }}
    </center>
    <br />
{% endblock %}
